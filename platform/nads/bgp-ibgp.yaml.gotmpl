apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: bgp-ibgp
  namespace: bitcoin-mcast
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "type": "macvlan",
      "master": "${BGP_IBGP_IFACE}",
      "mode": "bridge",
      "ipam": { "type": "static" }
    }
